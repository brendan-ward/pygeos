

### init
# if (init_geos(m) < 0) {
#     return NULL;
# };

# if (init_geom_type(m) < 0) {
#     return NULL;
# };

# if (init_strtree_type(m) < 0) {
#     return NULL;
# };


# d = PyModule_GetDict(m);
# import_array();
# import_umath();

# if (init_ufuncs(m, d) < 0) {
#     return NULL;
# };